import { Eye,EyeOff,LockKeyhole,Mail,UserRound } from 'lucide-react';import{useState}from'react';import{Link,useLocation,useNavigate}from'react-router-dom';import{errorMessage}from'../api';import{useAuth}from'../contexts/AuthContext';
export function LoginPage(){const[email,setEmail]=useState('');const[password,setPassword]=useState('');const[show,setShow]=useState(false);const[busy,setBusy]=useState(false);const[error,setError]=useState('');const{login}=useAuth();const nav=useNavigate();const loc=useLocation();const submit=async(e:React.FormEvent)=>{e.preventDefault();setError('');if(!email.trim()||!password){setError('Email dan password wajib diisi.');return}setBusy(true);try{const u=await login(email,password);const from=(loc.state as{from?:string}|null)?.from;nav(from||(u.role==='admin'||u.role==='staff'?'/admin':'/account'),{replace:true})}catch(x){setError(errorMessage(x))}finally{setBusy(false)}};return <AuthShell title="Selamat datang kembali" subtitle="Masuk untuk mengakses produk dan pesanan Anda."><form onSubmit={submit} noValidate><Field id="login-email" label="Email" icon={<Mail/>}><input id="login-email" name="email" type="email" autoComplete="email" value={email} onChange={e=>setEmail(e.target.value)} placeholder="email@contoh.com"/></Field><Field id="login-password" label="Password" icon={<LockKeyhole/>}><input id="login-password" name="password" type={show?'text':'password'} autoComplete="current-password" value={password} onChange={e=>setPassword(e.target.value)} placeholder="••••••••"/><button type="button" className="input-action" onClick={()=>setShow(!show)} aria-label="Tampilkan password">{show?<EyeOff/>:<Eye/>}</button></Field>{error&&<p className="form-error">{error}</p>}<button className="btn primary full" type="submit" disabled={busy}>{busy?'Memproses...':'Masuk'}</button></form><p className="auth-switch">Belum punya akun? <Link to="/register">Daftar sekarang</Link></p></AuthShell>}
export function RegisterPage(){const[name,setName]=useState('');const[email,setEmail]=useState('');const[password,setPassword]=useState('');const[confirm,setConfirm]=useState('');const[terms,setTerms]=useState(false);const[busy,setBusy]=useState(false);const[error,setError]=useState('');const{register}=useAuth();const nav=useNavigate();const submit=async(e:React.FormEvent)=>{e.preventDefault();setError('');if(name.trim().length<3)return setError('Nama minimal 3 karakter.');if(!/^\S+@\S+\.\S+$/.test(email))return setError('Format email tidak valid.');if(password.length<8||!/[A-Za-z]/.test(password)||!/[0-9]/.test(password))return setError('Password minimal 8 karakter dan memiliki huruf serta angka.');if(password!==confirm)return setError('Konfirmasi password tidak sama.');if(!terms)return setError('Anda harus menyetujui syarat dan kebijakan.');setBusy(true);try{await register(name,email,password);nav('/account',{replace:true})}catch(x){setError(errorMessage(x))}finally{setBusy(false)}};return <AuthShell title="Buat akun NAFI" subtitle="Mulai akses produk digital pilihan untuk kebutuhan Anda."><form onSubmit={submit} noValidate><Field id="register-name" label="Nama Lengkap" icon={<UserRound/>}><input id="register-name" name="fullName" autoComplete="name" value={name} onChange={e=>setName(e.target.value)} placeholder="Nama lengkap"/></Field><Field id="register-email" label="Email" icon={<Mail/>}><input id="register-email" name="email" type="email" autoComplete="email" value={email} onChange={e=>setEmail(e.target.value)} placeholder="email@contoh.com"/></Field><Field id="register-password" label="Password" icon={<LockKeyhole/>}><input id="register-password" name="password" type="password" autoComplete="new-password" value={password} onChange={e=>setPassword(e.target.value)} placeholder="Minimal 8 karakter"/></Field><Field id="register-confirm" label="Konfirmasi Password" icon={<LockKeyhole/>}><input id="register-confirm" name="confirmPassword" type="password" autoComplete="new-password" value={confirm} onChange={e=>setConfirm(e.target.value)} placeholder="Ulangi password"/></Field><label className="checkbox"><input id="register-terms" name="terms" type="checkbox" checked={terms} onChange={e=>setTerms(e.target.checked)}/><span>Saya menyetujui <Link to="/terms">syarat dan kebijakan</Link>.</span></label>{error&&<p className="form-error">{error}</p>}<button className="btn primary full" type="submit" disabled={busy}>{busy?'Mendaftarkan...':'Daftar Sekarang'}</button></form><p className="auth-switch">Sudah punya akun? <Link to="/login">Masuk di sini</Link></p></AuthShell>}
function Field({id,label,icon,children}:{id:string;label:string;icon:React.ReactNode;children:React.ReactNode}){return <label className="field" htmlFor={id}><span>{label}</span><div className="input-wrap">{icon}{children}</div></label>}
function AuthShell({title,subtitle,children}:{title:string;subtitle:string;children:React.ReactNode}){return <section className="auth-page"><div className="auth-card"><div className="brand auth-brand"><span className="brand-mark">N</span><span><b>NAFI Marketplace</b><small>AI THAT WORKS FOR YOU</small></span></div><h1>{title}</h1><p>{subtitle}</p>{children}</div></section>}
